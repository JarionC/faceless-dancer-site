{
  "name": "faceless-dancer-site",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "npm run build --workspace shared && concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace shared && npm run build --workspace server && npm run build --workspace client",
    "start": "npm run start --workspace server",
    "migrate": "npm run migrate --workspace server",
    "import:db": "npm run import:db --workspace server",
    "import:beat-storage": "npm run import:beat-storage --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
