## Instructions
1. Plan first, ask for approval, then make file/code changes
2. Put plans in docs/plans with distinct names
3. Be very modular in your coding and put all variables in env as well as runtime config(if developed)
4. don't use .env.example - edit the actual .env that the app uses